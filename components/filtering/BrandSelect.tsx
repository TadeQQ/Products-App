import React from "react";

interface ButtonProps {
  onClick: React.MouseEventHandler<HTMLButtonElement>;
}
export const BrandSelect = () => {
  return (
    <div>
      <button> Brand_Select</button>
    </div>
  );
};
