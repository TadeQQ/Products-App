import React from "react";

interface ButtonProps {
  onClick: React.MouseEventHandler<HTMLButtonElement>;
}
export const CategorySelect = () => {
  return (
    <div>
      <button> Category_Select</button>
    </div>
  );
};
